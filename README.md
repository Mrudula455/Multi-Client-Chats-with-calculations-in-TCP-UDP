# Multi-Client-Chats-with-calculations-in-TCP-UDP
This project demonstrates a client-server chat system using the ns-3 network simulator. Multiple clients send messages to a server using UDP sockets. The server processes the packets, calculates packet size and simulation time, and returns acknowledgements to the clients.
